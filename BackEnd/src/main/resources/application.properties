spring.application.name=BackEnd

spring.datasource.url=jdbc:mysql://localhost:3306/webkhoahocon
spring.datasource.username=root
spring.datasource.password=123456

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.database=mysql

spring.devtools.restart.enabled=false

app.jwt.secret=${JWT_SECRET:change-me-to-a-secure-jwt-secret-for-production}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

momo.partner-code=${MOMO_PARTNER_CODE:MOMO}
momo.access-key=${MOMO_ACCESS_KEY:F8BBA842ECF85}
momo.secret-key=${MOMO_SECRET_KEY:K951B6PE1waDMi640xX08PD3vg6EkVlz}
momo.endpoint=${MOMO_ENDPOINT:https://test-payment.momo.vn/v2/gateway/api/create}
# Frontend static server runs on :5000 in this workspace.
momo.redirect-url=${MOMO_REDIRECT_URL:http://localhost:5000/payment-result.html}
# For real IPN from MoMo, set MOMO_IPN_URL to a public HTTPS URL (e.g. via ngrok).
momo.ipn-url=${MOMO_IPN_URL:http://localhost:8080/api/payment/momo/ipn}
momo.request-type=${MOMO_REQUEST_TYPE:payWithMethod}
momo.lang=${MOMO_LANG:vi}
momo.order-info-prefix=${MOMO_ORDER_INFO_PREFIX:Thanh toan khoa hoc}
